<UserControl x:Class="Inventory.Views.InputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventory.Views" xmlns:viewmodels="clr-namespace:Inventory.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:InputViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Width="300">
            <Label>Device</Label>
            <TextBox Text="{Binding DeviceName, UpdateSourceTrigger=PropertyChanged}" />
            <Label>Last User</Label>
            <TextBox Text="{Binding LastUser, UpdateSourceTrigger=PropertyChanged}" />
            <Label>Status</Label>
            <ComboBox SelectedIndex="0" ItemsSource="{Binding DeviceStatusSelection}" SelectedItem="{Binding DeviceStatus, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

            <StackPanel  Margin="0,15,0,0" Orientation="Horizontal">
                <Button Command="{Binding SubmitCommand}">Submit</Button>
                <Button Margin="15,0,0,0" Style="{DynamicResource MaterialDesignToolButton}" Command="{Binding CancelCommand}">Cancel</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
